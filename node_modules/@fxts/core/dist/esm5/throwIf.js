import identity from "./identity.js";
function throwIf(predicate, err) {
    if (err === void 0) { err = identity; }
    return function (input) {
        if (predicate(input))
            throw err(input);
        return input;
    };
}
export default throwIf;
//# sourceMappingURL=throwIf.js.map