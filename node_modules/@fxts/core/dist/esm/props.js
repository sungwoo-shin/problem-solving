function props(keys, obj) {
    if (obj === null)
        return new Array(keys.length).fill(undefined);
    if (obj === undefined)
        return (obj) => props(keys, obj);
    const result = [];
    for (const key of keys)
        result.push(obj[key]);
    return result;
}
export default props;
//# sourceMappingURL=props.js.map