import type Include from "./types/Include";
/**
 * Returns true if `s` is a String.
 *
 * @example
 * ```ts
 * isString("a"); // true
 * isString(2); // false
 * ```
 */
declare const isString: <T>(s: T) => s is Include<T, string>;
export default isString;
//# sourceMappingURL=isString.d.ts.map