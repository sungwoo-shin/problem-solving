import type Include from "./types/Include";
/**
 * Checks if the given value is `null` or `undefined`.
 *
 * @example
 * ```ts
 * isNil(1); // false
 * isNil('1'); // false
 * isNil(undefined); // true
 * isNil(null); // true
 * ```
 */
declare const isNil: <T>(a: T) => a is Include<T, null | undefined>;
export default isNil;
//# sourceMappingURL=isNil.d.ts.map