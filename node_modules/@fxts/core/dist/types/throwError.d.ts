/**
 * throw result of `toError`
 *
 * @example
 * ```ts
 * const result: never = pipe(
 *   0,
 *
 *   throwError((input) => Error(`input is ${input}`)),
 *
 *   (input: never) => {
 *     // unreachable point
 *   },
 * );
 * ```
 */
declare const throwError: <T, E extends Error>(toError: (input: T) => E) => (input: T) => never;
export default throwError;
//# sourceMappingURL=throwError.d.ts.map